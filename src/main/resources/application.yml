spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aether
    username: postgres
    password: 852456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
